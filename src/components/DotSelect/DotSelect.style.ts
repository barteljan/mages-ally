import {StyleSheet} from 'react-native';

export const style = StyleSheet.create({
  container: {
    flexDirection: 'row',
  },
  dotContainer: {
    padding: 2,
  },
  spacer: {
    width: 8,
  },
});
