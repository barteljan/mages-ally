import {Theme} from 'react-native-paper';

export type CharactersScreenStyles = {};

export const makeCharactersScreenStyles = (
  // eslint-disable-next-line @typescript-eslint/no-unused-vars
  theme: Theme,
): CharactersScreenStyles => {
  return {};
};
