import {StyleSheet} from 'react-native';

export const style = StyleSheet.create({
  container: {
    flexDirection: 'row',
  },
  dotContainer: {
    padding: 2,
    flexWrap: 'wrap',
  },
  spacer: {
    width: 8,
  },
});
